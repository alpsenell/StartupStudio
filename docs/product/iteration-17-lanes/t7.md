# T7 — press and stakes

*Iteration 17, lane T7, branch `t7-press` from `scaffold-17`, simulator
`ws-l7`. Specs: `iteration-17-pm/player.md` §3 (P3, the exclusive) and
`genre.md` §4 (G4, a stake in a rival).*

## What was built

**The exclusive (P3), as the coordinator re-cut it.** The ship
confirmation offers *Exclusive to…* one of the four review outlets, or none
(`Screens/Products/ExclusiveRow.swift`). The row is mounted on the product
page's and the Products list's ship card, and in the war room above its
ship button. Both answers are printed before the tap:
- "TechDaily alone judges launch week · warm to you (+4). The other three
  publish 7 days later, so launch week's buyers read TechDaily's score —
  higher or lower than all four would have been. TechDaily warms +5, the
  other three cool −1 for your next launch."
- "Even-handed: all four publish on launch day, launch week reads their
  average, and no one's standing moves."

After the ship goes through, the host sends
`.grantExclusive(productID:outlet:)`, so the choice composes with every
way a build ships. It embargoes the other three verdicts for
`press.embargoDays` (7) through `ReleaseInfo.embargoUntilDay`:
- **Surfaces:** the launch-day sheet, the war room (reveal, stamp and the
  office's cheer), the storefront (card and stars), the product page
  (header badge and reviews card) and the Products list's score badge
  show only the exclusive's verdict until the day it lifts. An embargo row
  says who publishes when.
- **Journal:** launch day's line reads "TechDaily's verdict on Round 15 is
  in: 39. The others publish in a week."
- **Lift:** on the day, `PressSystem.run` posts `pressEmbargoLifted` ("The
  embargo lifts on Round 15: the other verdicts are in, and the average is
  N").
- **Launch week:** buyers read the exclusive's score alone.
  `ReleaseInfo.demandReviewScore` feeds `postWeeklySales`'s quality term,
  premium curve and overpriced check for the first week posted (or any week
  inside the embargo), and the average from then on.
- **Paper:** that week's paper leads with the exclusive's verdict and
  quotes its blurb.

Standing:
- The chosen outlet warms +5 and the other three cool −1 (option (b),
  snub 2 → 1, so an exclusive nets +2 standing).
- Standing is clamped to ±20, adds `standing / 3` to that outlet's score
  on every later launch, and drifts 1 toward 0 per launch.
- Once any outlet has a standing, every review card prints a byline in
  words and points: "warm to you · +4 on your scores", "friendly",
  "even-handed", "cool on you", "cold on you".

With no exclusive, nothing above happens: all four land on launch day
exactly as today, and every read is byte-identical.

- Engine: `Company.pressStanding: [String: Double]` (encoded as an
  outlet-sorted array, and only when non-empty) and
  `ReleaseInfo.exclusiveOutlet: String?` and `embargoUntilDay: Int?`
  (neither written while nil). `ReleaseInfo.visibleReviews(on:)`,
  `visibleAverageScore(on:)`, `embargoedOutlets(on:)` and
  `demandReviewScore(on:)` live in `Press.swift`. `PressSystem.run` (the
  lift, in the Reducer's T7 systems region) finds nothing on a run with
  no exclusive and draws nothing.
  `Press.swift` holds `pressScoreOffset`, the `PressStandingBand` words and
  `pressExclusiveRefusal`. `PressSystem` holds `grantExclusive` and
  `driftAfterLaunch`.
- `ProductSystem.swift` (T2's): two adjacent pairs in `ship` (see
  Deviations) and one in `postWeeklySales`. That one computes
  `pressDemandScore`, and the three lines that read `averageReviewScore`
  now read it; each carries a `// T7` tag. It is exactly the average
  without an exclusive.
- App: `ReviewRevealList` puts the exclusive first; `ReviewCardView` gains
  the byline and the stamp; `PressByline` lives in `LaunchDaySheet.swift`.
  The war room's reveal passes the bylines too. `NewspaperComposer` gets a
  lead-rank block. There are four `EventCopy` lines.

**A stake in a rival (G4).** On the rival's profile, under *Acquire* and
K4's paper rows, *Or buy a stake* offers three buttons, 5%, 10% and 25%,
each priced at `valuation × 1.1 × percent`. They are refused inside an
earn-out, with the for-sale sign up, after the ending, while you already
hold a stake in them, or without the cash. The caption prints what the 25%
stake does now and later: the strength it gives them, this week's
dividend, the roadmap from 10%, the poach and war effects, the 0.9×
sell-back and the fold.

Buying posts to the ledger, adds `price × 0.25 / 4000` to their strength
and records a `RivalStake { rivalID, percent, paid, sinceDay, dividends }`.

While held:
- **Dividend:** a weekly dividend is posted to the ledger ("Dividend: 25%
  of Vantage Point") on the rivals' weekly pass, after the folds.
- **Roadmap:** from 10%, "Their next launch: Fitness" appears on the
  profile (`focusTopicIDs.first`, the same line the mole's report reads).
- **Poaching:** their poaching odds are ×0.5. This applies only when they
  are the poacher, and the draw is unchanged.
- **Price war:** they start no price war with you, and their weeks-beaten
  count resets.

*Sell 25% for $X* is behind a confirmation. A fold removes the stake with a
`rivalStakeLost` event. `acquireRival` costs `(1 − percent) ×` the price;
the profile's acquisition terms mirror it, and absorbing the rival closes
the stake. The Rivals segment leads with a *Your stakes* section (a *What you
hold* card, one line per stake) once one is held, and Finances shows a *Stakes in rivals* card with value today, what
was paid, dividends and the sell-back.

- Engine: `RivalStakes.swift` (the stake, the quote, the reads) and
  `Systems/RivalSystem+Stakes.swift` (buy, sell, the week, the fold, the
  absorb). `RivalsState.stakes` is encoded only when non-empty.
- Marked pairs in T4's `RivalSystem.swift`: `run` (the dividend),
  `evolve`'s fold pass, `poachCheck`, `acquireRival`, `absorbRival` and
  `priceWarCheck`. `priceWarCheck` calls
  `rivalMarketStakeRefusesWar`, which lives in
  `RivalSystem+RivalMarket.swift`.
- App: `Screens/Business/RivalStakeRows.swift` holds `RivalStakeRows`,
  `RivalHoldingsCard` and `RivalStakeAssetCard`, with marked pairs in
  `RivalProfileScreen`, `RivalsView` and `FinancesView`.

**Actions** (T7 region): `grantExclusive`, `buyRivalStake(rivalID:percent:)`,
`sellRivalStake(rivalID:)`, `pressStakeDebugSeed(scenario:)` (debug only).
**Events**: `pressExclusive`, `pressEmbargoLifted`, `rivalStakeBought`, `rivalStakeSold`,
`rivalStakeLost` (all `.info` through the severity switch's default; the
journal files them under the company).
**Balance**: `press { exclusiveGain 5, snub 1, standingDivisor 3,
driftPerLaunch 1, cap 20, embargoDays 7 }` and `stakes { percents [0.05, 0.10, 0.25],
premium 1.1, sellBack 0.9, strengthShare 0.25, roadmapFrom 0.10,
poachFactor 0.5, countOffMarket false, dividendPayout 0.2 }` in
`BalanceConfig+Press.swift`, appended at the end of `Balance.json`.

## Measurements (first, before any tuning)

**P3 — is standing visible under the noise?** `reviewNoiseSigma` is
**6.0**, which is exactly the PM's failure threshold ("6 or more"). Each
outlet's spread against its product's mean was measured over every review
set in the committed fixtures (a Python walk over every `reviews` array):

| fixture | review sets | SD of outlet − product mean | range |
|---|---|---|---|
| release-campus-day900 | 49 | 4.99 | 29.2 |
| release-studio-day400 | 14 | 5.26 | 23.0 |
| release-bankruptcy | 23 | 5.86 | 28.5 |
| l3-family-day900 | 49 | 5.08 | 29.2 |
| all (544 reviews) | 136 | **5.19** | — |

The four outlets have no persona bias: their mean deviations are −0.35
(TechDaily), −0.01 (AppVerdict), +0.05 (The Stack Review) and +0.31
(ByteSized), with SDs of 4.5 to 5.7. ±4 at the cap sits inside one
standard deviation. The check fails, so the spec's remedy applies. The
byline says the standing in words, and the offset rises to
`standing / 3` (±6.7 at the cap, a point per three standing).

**P3 — what the embargo's week one is worth** (the coordinator's ask). On
the studio fixture (`release-studio-day400`), the most finished shippable
build, Round 15, was shipped five ways — with no exclusive and with each
outlet's — and ticked to its second posted week. Same harness, same
seed; the grant draws nothing.

| exclusive | its score | week one | week two |
|---|---|---|---|
| none (average 37) | — | 24 units, $95 | 61 units, $243 |
| TechDaily | 35 | 24 units, $95 | 61 units, $243 |
| AppVerdict | 36 | 24 units, $95 | 61 units, $243 |
| The Stack Review | 40 | 25 units, $99 | 61 units, $243 |
| ByteSized | 37 | 24 units, $95 | 61 units, $243 |

The gamble is real but small on this build. The outlets spread only 5
points here. Demand reads `0.4 + 0.6 × score / 100`, so at a 37 each
review point moves launch-week demand by about 1%. Week one is also the
thinnest week of the adoption ramp (24 units against week two's 61). A
one-SD outlet (±5 points on the 5.19 spread measured above) is about ±5%
of week one. Budget and standard tiers feel only that. Premium adds the
J5 demand curve and the overpriced check, which both now read the same
score. The lever grows with the standing: a warm outlet at +6.7 pulls week
one up by about 7% at no noise.

**G4 — is the stake free money?** Harness (a scratch SwiftPM executable
against this branch's engine, not committed):
- the shipped balance with rivals on;
- the ten pacing seeds;
- two pacing bots;
- at day 400, 25% of the strongest rival bought, with cash topped up to
  the price (neither bot ever has it);
- marked to market at day 730.

With the spec's dividend (the percent of their full weekly revenue):

| bot | runs | mark > paid | mark + dividends > paid | sale + dividends > paid | dividend yield |
|---|---|---|---|---|---|
| CrunchHire | 8 | **3/8** | 6/8 | 5/8 | 19–199% a year |
| SaaSBuilder | 7 | **2/7** | 6/7 | 6/7 | 72–279% a year |

(Runs that ended before day 400 are excluded. One stake on seed 20 002 was
lost to a fold.)

The PM's check, mark-to-market up on more than 8 of 10, does **not** fail:
the strongest rival's valuation fell on most seeds, often by half. So the
premium stays 1.1 and the sell-back 0.9. The check the PM feared the other
way round, a dividend under 2% a year on the campus, fails the other way.
A 25% stake in the campus's Vantage Point costs $178,110 and would pay
$8,742 a week, **255% a year**. Signal & Stone would pay 136%. The stake
was free money through the dividend, not the mark. A rival's valuation
(`strength × 4000 × (1 + rep/100)`) is small beside what its products sell.

**Tune:** `stakes.dividendPayout 0.2`, the share of their takings a studio
pays out. This is a new key, not in the spec. Re-measured:

| bot | mark > paid | mark + dividends > paid | sale + dividends > paid | dividend yield |
|---|---|---|---|---|
| CrunchHire | 3/8 | 4/8 | 2/8 | 7–31% a year |
| SaaSBuilder | 2/7 | 4/7 | 4/7 | 15–56% a year |
| campus | — | — | — | Vantage Point 51%, Signal & Stone 27% a year |

Now a stake is a bet on the studio: about half pay back within the 330
days, and the rest lose on the mark. The on-market dividend is nowhere
near 2%, so `countOffMarket` stays false. (Two harness runs end
bankrupt soon after buying, on seeds 4 242 and 55 055 for CrunchHire. The
topped-up cash was spent on the stake; that is a harness artefact, not the
game.)

## Deviations, and the repo fact

1. **The verdicts already land on launch day.** The spec says the exclusive
   outlet's review is revealed on launch day "instead of a week later", and
   that "Review copies are out. The verdicts land next week" is the whole
   of launch day. In the repo, `ProductSystem.ship` writes all four reviews
   at ship, and `LaunchDaySheet` and the war room reveal all four that day.
   The "next week" card only shows for an empty review list, which never
   happens with outlets configured. The repo wins, so the exclusive's
   day-one value does not exist. What it does:
   - the outlet's card leads the reveal with an `EXCLUSIVE` stamp;
   - its verdict and blurb lead that week's paper;
   - the standing moves.

   With the spec's +5 against −2 × 3, an exclusive netted −1 standing, a
   small price paid for the paper alone. I raised it mid-lane, and the
   **coordinator decided:**
   - the embargo, only when an exclusive is chosen (the three wait 7 days);
   - launch week's demand reads the exclusive's score;
   - snub 1.

   All of that is built as above. The default path is untouched.
2. **Two adjacent pairs in `ship`, not one.** The standing offset wraps the
   noise line inside the loop:
   `nextGaussian(...) + pressScoreOffset(...)`, exactly `+ 0.0` with an
   empty map, with the same one draw per outlet. The drift is one call
   right after the loop. They are ten lines apart. The loop's draws are
   unchanged in count and order.
3. **The exclusive is its own action, sent right after the ship.** It is not an
   argument on `ship`, so it merges with T2's `shipAt` / `shipReplacingAt`
   without touching their signatures. The review loop is unchanged. The
   grant only records the outlet, sets the embargo day and moves standing.
   The engine grants it only on launch day, once, to an outlet that
   reviewed the build, and nothing but the ship confirmation sends it.
   Because the scores are rolled at ship, a crafted action sent after
   reading them could pick the best outlet. The UI never offers that;
   carrying the outlet on the ship action itself would close it, and is
   a merge-time option with T2's `shipAt`.
4. **Standing / 3** instead of 0.2 per point (the measured remedy).
   `press.standingDivisor` replaces the spec's `scorePerStanding`.
5. **`stakes.dividendPayout 0.2`** is new (the measured tune above).
6. **The stake lives in `RivalStakes.swift`**, because `Stakes.swift` is
   iteration 8's ladder. The first draft overwrote that file; it was
   restored from git before the first commit.
7. **Holdings lead the Rivals segment** (after K4's sign when it leads) and
   the asset card sits after the run rate on Finances, so both are on the
   first screen.

## Identity

- No bot sends a new action.
- `pressStanding` and `stakes` stay empty and are never encoded.
- The review offset is exactly 0.0, and the drift and the stake week
  return on their first line.
- The poach factor is exactly 1 and the acquisition price is the full
  price.
- The war-refusal check is false.
- There are no new `rng` / `worldRNG` draws on any path.
- The engine, release and pacing fixtures do not move.

## Suites

- **Engine:** ✔ Test run with 943 tests.
- **Content:** ✔ Test run with 52 tests.
- **Save:** ✔ Test run with 34 tests.
- **PixelKit:** ✔ Test run with 336 tests. The first run failed `OfficePerfTests.repeatedFramesRasterizeNothing` (rasterized 33, expected 0) while other lanes were building. It passed clean on a solo re-run, and this lane changes nothing in PixelKit.
- **App:** SUITES-APP

No test was added.

## Re-pins

REPINS

## Merge dry run

Run with `git merge-tree --write-tree t7-press <branch>` at tip
`6282124`. T1 has 2 commits past `scaffold-17`, T2 1, T3 3, T4 2 and T5 1;
T6 and `iteration-17` have none. Every CONFLICT line:

- **t1-exits** — `CONFLICT (content): Merge conflict in
  Packages/TycoonEngine/Sources/TycoonEngine/Resources/Balance.json`. Both
  sides append a block after `"seating"`. Keep both, each with a trailing
  comma except the last.
- **t2-build** — `CONFLICT (content): Merge conflict in
  Packages/TycoonEngine/Sources/TycoonEngine/Resources/Balance.json`. Same
  as above: keep both appended blocks.
- **t2-build** — `CONFLICT (content): Merge conflict in
  App/Sources/Screens/Products/ProductDetailScreen.swift`. T2 replaced the
  ship `confirmationDialog` with `.sheet { ShipSheet(...) }`, and this
  lane's two pairs sit in that area: the `ExclusiveRow` under the Ship
  button and the `grantAfterShip` inside the dialog's *Ship it*.
  **Resolution:** take T2's side; drop both of this lane's pairs in the
  file. Then move the row onto T2's sheet, one home per thing. In
  `ShipSheet.swift`:
  - in `body`, after `tiers`, add
    `// MARK: T7 … ExclusiveRow(engine: engine, productID: productID) // MARK: end T7`;
  - in `ship(replacing:)`, right after `shell.toasts.send(action, to: engine, rejected: rejected)`
    and before `dismiss()`, add
    `ExclusivePick.shared.grantAfterShip(productID: productID, engine: engine)`.

  That covers the plain ship, the priced ship and the replacement.
- **t2-build** — `CONFLICT (content): Merge conflict in
  App/Sources/Screens/Products/ProductsListView.swift`. Same resolution:
  take T2's side and drop this lane's two pairs there (the row above
  `WarRoomEntryButton` and the grant in the removed dialog). The row and the
  grant live on `ShipSheet` as above.
- **t3-people** — `CONFLICT (content): Merge conflict in
  Packages/TycoonEngine/Sources/TycoonEngine/Resources/Balance.json`. Keep
  both appended blocks.
- **t4-publisher** — `CONFLICT (content): Merge conflict in
  App/Sources/Screens/Business/RivalProfileScreen.swift`. Both lanes add a
  pair right after K4's `DealProfileRows` in `RivalDealCard`: T4's
  `PublisherProfileRows`, and T7's `RivalStakeRows` with its debug sheet.
  **Keep both, T4's first**, following the merge order (T4 merges before
  T7).
- **t4-publisher** — `CONFLICT (content): Merge conflict in
  App/Sources/Screens/Story/NewspaperComposer.swift`. Both lanes add a
  lead-rank pair after K2's in `leadRank`. **Keep both, T4's first**; the
  two match different events, so the order does not change a rank.
- **t4-publisher** — `CONFLICT (content): Merge conflict in
  Packages/TycoonEngine/Sources/TycoonEngine/Resources/Balance.json`. Keep
  both appended blocks.
- **t5-expo** — `CONFLICT (content): Merge conflict in
  Packages/TycoonEngine/Sources/TycoonEngine/Resources/Balance.json`. Keep
  both appended blocks.

The scaffolded regions (`GameAction`, `Reducer`, `GameState`,
`BalanceConfig`, `EventCopy`, `DebugLaunch`, `BusinessScreen`) merged
cleanly against all five branches with commits. The pairs in T4's
`RivalSystem.swift` and `Rival.swift` and in T2's `ProductSystem.swift`
merged cleanly too. So did the pairs in `LaunchDaySheet.swift`, which T5
touched. T6 has no commits yet, so its "you were away" pair on the launch
sheet is unchecked. The sheet's body rows are unchanged; this lane's
changes are inside `ReviewRevealList`, `ReviewCardView` and appended
helpers.

## Follow-ups

- **Where the embargo does not reach yet.** Every `averageReviewScore`
  read outside the named surfaces still sees all four during the week:
  - the engine's reputation nudge and the category ledger at ship (the
    press filed; the public reads a week later);
  - the rivals' head-to-head rows and the acquisition terms;
  - the decision sheet's comparison;
  - the biography and share cards;
  - the lifecycle price sheet's caption.
- **Launch-day scene:** a `pressEmbargoLifted` day does not reopen the
  launch sheet. The shell opens it on `.shipped` and `.reviewsIn`, and
  `GameShell` is not this lane's file; the journal and toast carry the
  lift.
- **Week-one lever size:** the exclusive's week-one effect is about 1%
  per review point. That is honest but small; a larger lever (the review
  term's weight in launch week) is an owner call.
- **Paper acquisitions:** `acquireRivalForStock` (K4's paper deal) closes a
  held stake without discounting the paper. Only the cash acquisition
  counts the stake.
- **Valuation:** stakes are not in `companyValuation` (it would change the
  buyout and IPO numbers). The asset card says so.
- **Replacing a live product:** the war room still has no *Replace* (K2's
  follow-up). In this tree the exclusive also only rides *Ship it*, not the
  K2 replace button; on T2's sheet it rides every answer.
- **Stake events and the journal:** the stake events are company news in the
  journal. `rivalStakeLost` might deserve `.notable`, but that switch has
  no T region.
- **A war already running:** a price war the rival started before you bought in runs its course. The stake stops the next one, not a current one (seen on the campus: Vantage Point's war in Music).
- **Next topic:** the roadmap line reads `focusTopicIDs.first`, as the mole
  does. A studio that moved in on a boom may launch elsewhere
  (`rivalMarketLaunchTopic`).

## Debug flags

`-autoRoute t7-…` is dressed once from the Business tab (`BusinessScreen`
T7 region, `DebugLaunch.t7DressIfAsked`):

| route | fixture | what it shows |
|---|---|---|
| `t7-plain` | release-studio-day400 | the plain ship and launch sheet (the default path, as before) |
| `t7-launch` | release-studio-day400 | the same ship with the four outlets dressed warm to cold (the bylines) |
| `t7-exclusive` | release-studio-day400 | the ship with the first outlet given the exclusive |
| `t7-paper` | release-studio-day400 | the exclusive a week later, on the newspaper |
| `t7-offer` | release-campus-day900 | the strongest rival's stake rows, lifted into a sheet |
| `t7-stake` | release-campus-day900 | a held 25% stake, lifted into a sheet |
| `t7-holdings` | release-campus-day900 | Rivals with the holdings card |
| `t7-finances` | release-campus-day900 | Finances with the asset card |

Each is launched with `-unlocked -autoFixture <fixture> -autoTab business
-autoRoute <route>`.

## Screenshots

Under `t7/`:

- `before-launchday.png` (`t7-plain`): the default launch sheet, all four
  verdicts on launch day, no bylines. This is what the scaffold shows.
- `after-launchday-bylines.png` (`t7-launch`): the same ship with the
  outlets dressed; each card prints its standing in words and points.
- `after-launchday-exclusive.png` (`t7-exclusive`): TechDaily first, with
  the `EXCLUSIVE` stamp, warmer after the grant; the others cooler.
- `after-ship-row.png`: *Exclusive to… none* under *Ship it* on the
  Products list, with the even-handed answer printed.
- `after-paper.png` (`t7-paper`): the next issue leads "TechDaily had Round
  15 first and gave it 39", with the blurb as the deck.
- `after-stake-offer.png` (`t7-offer`): Vantage Point's 5/10/25% buttons,
  priced, with the terms caption.
- `after-stake-held.png` (`t7-stake`): 25% held — paid, worth today, the
  week's dividend, *Their next launch: Travel*, *Sell 25% for $150,134*.
- `after-holdings.png` (`t7-holdings`): *Your stakes* leading the Rivals
  segment. This shot has the earlier "Your stakes" card title; it is now
  *What you hold*.
- `after-finances.png` (`t7-finances`): *Stakes in rivals* after the run
  rate.

No before shots exist for the stake surfaces or the ship row; before this
lane there was nothing there.
