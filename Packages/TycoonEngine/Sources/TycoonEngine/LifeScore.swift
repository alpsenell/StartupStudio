import Foundation

// Iteration 9 — L2 owns this file and may reshape it freely.

/// The founder's life, graded 0...100 as a pure function of the state:
/// the second number next to net worth. Scaffold stub: L2 writes the
/// real grading and its breakdown.
public enum LifeScore {
    public static func score(_ state: GameState, balance: BalanceConfig) -> Int {
        0
    }
}
