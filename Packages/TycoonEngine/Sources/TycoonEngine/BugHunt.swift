import Foundation

// Iteration 10 — M6 owns this file and may reshape it freely. The tactile
// bug hunt: bugs crawl across the coders' desks in the pixel office
// during a build and a tap squashes one. The engine side is one action
// (`.squashBug(productID:)` in M6's `GameAction` region) against
// `DevProgress.openBugs`, with a per-day cap so it is a treat, not a
// strategy; the office scene side is PixelKit's `OfficeFX`.

public enum BugHunt {
    /// Placeholder so the file compiles; M6 replaces it.
    public static let placeholder = true
}
